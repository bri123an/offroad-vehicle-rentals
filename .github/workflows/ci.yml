name: CI - Build and Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore OffroadVehicleRentals.sln

    - name: Build Solution
      run: dotnet build OffroadVehicleRentals.sln --no-restore --configuration Release

    - name: Test
      run: dotnet test OffroadVehicleRentals.sln --no-build --configuration Release --verbosity normal
      continue-on-error: true

    - name: Upload build artifacts - API
      uses: actions/upload-artifact@v4
      with:
        name: api-build
        path: ./src/OffroadVehicleRentals.Api/bin/Release/net8.0/
        retention-days: 1

    - name: Upload build artifacts - Web
      uses: actions/upload-artifact@v4
      with:
        name: web-build
        path: ./src/OffroadVehicleRentals.Web/bin/Release/net8.0/
        retention-days: 1
